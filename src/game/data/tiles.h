#include "defines.h"

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(0)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(1)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(2)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(3)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(4)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(5)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(6)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(7)
    SOLID()
)




TILE(air,
    SIMPLE_STATIONARY_TEXTURE(8)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(9)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(10)
)










TILE(air,
    SIMPLE_STATIONARY_TEXTURE(11)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(12)
    SOLID()
)
TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(13)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(14)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(15)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(16)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(17)
    SOLID()
)




TILE(air,
    SIMPLE_STATIONARY_TEXTURE(18)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(19)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(20)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(21)
    SOLID()
)



TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(22)
    SOLID()
)
TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(23)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(24)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(25)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(26)
)



TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(27)
    SOLID()
)
TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(28)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(29)
)

TILE(ground,
    SIMPLE_STATIONARY_TEXTURE(30)
    SOLID()
)

TILE(air,
    SIMPLE_STATIONARY_TEXTURE(31)
)
